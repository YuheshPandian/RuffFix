name: Test RuffFix Action

on:
  push:
    branches:
      - add_mode_feature

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test RuffFix
        uses: ./  # assumes your action.yml is in repo root
        with:
          mode: lint

